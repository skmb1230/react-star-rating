{
  "compilerOptions": {
    "declaration": true, // 타입 선언 파일 생성
    "declarationDir": "dist", // 선언 파일이 저장될 디렉토리
    "emitDeclarationOnly": true, // JS 파일은 생성하지 않고 타입 정의 파일만 생성
    "outDir": "dist", // 빌드 결과물 출력 디렉토리
    "jsx": "react-jsx", // React JSX 설정
    "module": "ESNext", // ES 모듈 형식 사용
    "moduleResolution": "node" // Node.js 모듈 해석 방식
  },
  "include": ["src"], // 타입 정의 파일 생성 대상
  "exclude": ["node_modules", "dist"] // 제외할 경로
}
